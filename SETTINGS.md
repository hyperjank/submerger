# Project Settings

This file documents configuration options stored in `settings.json`.

## LLM settings

```
{
  "llm": {
    "api_key": "<your token>",
    "api_base": "http://localhost:1234/v1",
    "model": "deepseek-chat"
  }
}
```

- `api_key` – API token for the language model service.
- `api_base` – HTTP endpoint of the API.
- `model` – Model name to use.

## Player settings *(placeholder)*

Player specific options such as default window size or playback shortcuts.

## Dictionary settings *(placeholder)*

Options for offline dictionary lookups and path to dictionary files.

## Plugin settings *(placeholder)*

Paths and configuration for optional learning tools.
